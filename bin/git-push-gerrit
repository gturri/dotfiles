#!/usr/bin/env bash

branch=master
if [ $# -gt 0 ]; then
  branch=$1
fi

if [ $# -gt 1 ]; then
 postfix="%topic=$2"
fi

git push gerrit HEAD:refs/publish/$branch$postfix
ssh -p 29418 review.criteois.lan gerrit review --ready-to-submit 1 `git rev-parse HEAD`
