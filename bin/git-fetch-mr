#!/usr/bin/env bash

if [ $# -ne 1 ]; then
  echo "expected the number of the merge request"
  exit -1
fi

idMR=$1

git fetch origin refs/merge-requests/${idMR}
git checkout -b mr/${idMR} FETCH_HEAD


