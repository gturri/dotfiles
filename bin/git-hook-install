#!/usr/bin/env bash

if [ ! -d .git ]; then
  echo "Should be run from the root of a git repo"
  exit 1
fi

cp $DOTFILES_ROOT/hooks/* .git/hooks
gitdir=$(git rev-parse --git-dir); scp -p -P 29418 g.turri@review.crto.in:hooks/commit-msg ${gitdir}/hooks/

